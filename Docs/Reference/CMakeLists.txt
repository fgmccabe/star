cmake_minimum_required(VERSION 3.7)
project(Reference)

include(../makeinfo.cmake)

add_info(ref intro lexical grammar programs types patterns expressions conditions queries actions tasks standard numbers strings collections catalog)

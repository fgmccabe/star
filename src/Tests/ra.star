test.ra{
  import star.
  import test.ralist.

  T:ra[string].
  T = ["alpha","beta","gamma","delta","eta","one","two","three","four","five"].

  show dump(T).

  show disp(T[0]).
  show disp(T[1]).
  show disp(T[2]).
  show disp(T[3]).
  show disp(T[4]).
  show disp(T[5]).
  show disp(T[6]).
  show disp(T[7]).
  show disp(T[8]).
  show disp(T[9]).
  show disp(T[10]).
}
